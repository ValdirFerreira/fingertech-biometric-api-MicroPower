// Ponto de entrada: UI/App.xaml.cs  (classe BiometricService.UI.App)
// O ASP.NET Core e iniciado em App.OnStartup() via Task.Run(StartApiAsync).
// Este arquivo existe apenas para referencia e nao e compilado como entry point.
